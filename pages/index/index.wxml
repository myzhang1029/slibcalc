<!--index.wxml-->
<view class="container">
  <view wx:for="{{array}}" class="btn-area">
    <navigator url="../{{cpage[item]}}/{{cpage[item]}}">
      <button type="primary" plain="true">
        {{functions[item]}}
      </button>
    </navigator>
  </view>
</view>
